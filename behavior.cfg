time_total    = 10            # acquisition time (seconds)
formatindex   = 1              # binning of the camera sensor
exposure      = 4000           # exposure of the sensor (microseconds)
gain          = 4.0            # gain applied to the sensor
time_tick     = 32.0           # basic tick time of the camera (nanoseconds)
left          = 0              # cropping left
top           = 0              # cropping top
height        = 494            # height of video
width         = 656            # width of video (should be a multiple of 4 or 32)
subsample     = 1              # further binning
display_speed = 20             # display every n frames
fps           = 150            # frames per second
result_dir    = D:\            # result directory